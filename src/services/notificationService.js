export const showSuccess = (message) => ({ type: 'success', message });
export const showError = (message) => ({ type: 'error', message });
export const showWarning = (message) => ({ type: 'warning', message });
